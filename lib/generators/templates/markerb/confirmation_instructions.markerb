Welcome <%= @resource.send(@resource.class.authentication_keys.first) %>!

You can confirm your account through the link below:

[Confirm my account](<%= confirmation_url(@resource, confirmation_token: @token) %>)
